# Copy to .env — never commit real values.
# Phase 1 needs no variables: the web app runs on mock data.

# --- Phase 2: database (matches docker-compose.yml) ---
# DATABASE_URL=postgresql://jat:jat@localhost:5432/job_tracker
# API_INTERNAL_URL=http://localhost:4000
